const HOME = "/";

const JOIN = "/member/join";
const CHANGE_PASSWORD = "/member/changePassword";
const CHANGE_NICKNAME = "/member/changeNickname";

const LOGIN = "/login"

export{HOME, JOIN, CHANGE_NICKNAME, CHANGE_PASSWORD, LOGIN}